import type { Meta, StoryObj } from '@storybook/react'

import NewRoundPage from './NewRoundPage'

const meta: Meta<typeof NewRoundPage> = {
  component: NewRoundPage,
}

export default meta

type Story = StoryObj<typeof NewRoundPage>

export const Primary: Story = {}
