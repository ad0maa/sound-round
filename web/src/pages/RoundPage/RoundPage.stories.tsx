import type { Meta, StoryObj } from '@storybook/react'

import RoundPage from './RoundPage'

const meta: Meta<typeof RoundPage> = {
  component: RoundPage,
}

export default meta

type Story = StoryObj<typeof RoundPage>

export const Primary: Story = {}
